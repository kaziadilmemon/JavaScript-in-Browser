/* Window Object: window object represents browser window and provides methods to control it. It is a global Object.*/
console.log(window)

// Document Object Model (DOM) : DOM represents the page content as HTML.
console.log(document.body) // Page Body as JS object.
document.body.style.background="Yellow" // Changes page background to Yellow

/* Browser Object Model (BOM) : The Browser object model represents additional objects provided by the browser (host environment) for working with everyting except the document. 
The functions alert/confirm/prompt are also a part of BOM */
 // like we can use location href

